#version 460
#extension GL_EXT_ray_tracing: require
layout (binding = 0, rgba8) uniform image2D colorBuffer;

void main() {
    vec3 c = vec3(fract(gl_LaunchIDEXT.xy/10.),1.);
    imageStore(colorBuffer, ivec2(gl_LaunchIDEXT.xy), vec4(c, 1.0));
}